apiVersion: v1
kind: Service
metadata:
  name: {{ include "faaast-registry.fullname" . }}
  labels:
    {{- include "faaast-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "faaast-registry.selectorLabels" . | nindent 4 }}
